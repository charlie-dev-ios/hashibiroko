name: Plan Phase - 実装計画

on:
  pull_request:
    types: [closed]
    branches: [main]

jobs:
  setup-plan-phase:
    if: github.event.pull_request.merged == true && contains(github.event.pull_request.head.ref, '/spec')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Extract feature info from branch
        id: extract
        run: |
          BRANCH="${{ github.event.pull_request.head.ref }}"
          FEATURE_PREFIX=$(echo "$BRANCH" | sed 's|/spec$||')
          FEATURE_NAME=$(echo "$FEATURE_PREFIX" | sed 's|^[0-9]*-||')

          echo "feature_prefix=$FEATURE_PREFIX" >> $GITHUB_OUTPUT
          echo "feature_name=$FEATURE_NAME" >> $GITHUB_OUTPUT
          echo "plan_branch=${FEATURE_PREFIX}/plan" >> $GITHUB_OUTPUT

      - name: Find parent issue
        id: find-parent
        uses: actions/github-script@v7
        with:
          script: |
            const prBody = context.payload.pull_request.body;
            const match = prBody.match(/親Issue: #(\d+)/);
            if (match) {
              core.setOutput('parent_issue', match[1]);
            } else {
              core.setOutput('parent_issue', '');
            }

      - name: Create plan branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b ${{ steps.extract.outputs.plan_branch }}

          # Create placeholder
          mkdir -p specs/${{ steps.extract.outputs.feature_prefix }}
          touch specs/${{ steps.extract.outputs.feature_prefix }}/.plan-phase

          git add .
          git commit -m "chore: 実装計画ブランチを作成" --allow-empty
          git push -u origin ${{ steps.extract.outputs.plan_branch }}

      - name: Create plan sub-issue with instructions
        id: create-sub-issue
        uses: actions/github-script@v7
        with:
          script: |
            const branchName = '${{ steps.extract.outputs.plan_branch }}';
            const featureName = '${{ steps.extract.outputs.feature_name }}';
            const parentIssue = '${{ steps.find-parent.outputs.parent_issue }}';

            const instructions = `## 実装計画フェーズ

            親Issue: #${parentIssue}
            ブランチ: \`${branchName}\`

            ### 実行手順

            #### 1. ブランチをチェックアウト
            \`\`\`bash
            git fetch origin
            git checkout ${branchName}
            \`\`\`

            #### 2. spec-kit コマンドを実行
            Claude Code で以下を実行:
            \`\`\`
            /plan
            /task
            /analyze
            \`\`\`

            #### 3. 変更をコミット・プッシュ
            \`\`\`bash
            git add .
            git commit -m "docs(plan): 実装計画とタスクリストを作成"
            git push
            \`\`\`

            #### 4. PRを作成
            プッシュ後、自動的にPRが作成されます。

            ---
            ### ステータス
            ⏳ 手動実行待ち`;

            const { data: subIssue } = await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `[Plan] ${featureName}`,
              body: instructions,
              labels: ['phase:plan', 'status:in-progress']
            });

            core.setOutput('sub_issue_number', subIssue.number);

      - name: Comment on parent issue
        if: steps.find-parent.outputs.parent_issue != ''
        uses: actions/github-script@v7
        with:
          script: |
            const parentIssue = ${{ steps.find-parent.outputs.parent_issue }};
            const subIssue = ${{ steps.create-sub-issue.outputs.sub_issue_number }};
            const branchName = '${{ steps.extract.outputs.plan_branch }}';

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: parentIssue,
              body: `## 📋 実装計画フェーズを開始しました

            - Sub-issue: #${subIssue}
            - ブランチ: \`${branchName}\`

            詳細な手順は Sub-issue を確認してください。`
            });

name: Plan Phase - 実装計画

on:
  pull_request:
    types: [closed]
    branches: [main]

jobs:
  start-plan-phase:
    if: github.event.pull_request.merged == true && startsWith(github.event.pull_request.head.ref, '') && contains(github.event.pull_request.head.ref, '/spec')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Claude Code CLI
        run: npm install -g @anthropic-ai/claude-code

      - name: Extract feature info from branch
        id: extract
        run: |
          BRANCH="${{ github.event.pull_request.head.ref }}"
          # Extract feature prefix (e.g., 001-feature-name from 001-feature-name/spec)
          FEATURE_PREFIX=$(echo "$BRANCH" | sed 's|/spec$||')
          echo "feature_prefix=$FEATURE_PREFIX" >> $GITHUB_OUTPUT
          echo "plan_branch=${FEATURE_PREFIX}/plan" >> $GITHUB_OUTPUT

      - name: Find parent issue
        id: find-parent
        uses: actions/github-script@v7
        with:
          script: |
            const prBody = context.payload.pull_request.body;
            const match = prBody.match(/親Issue: #(\d+)/);
            if (match) {
              core.setOutput('parent_issue', match[1]);
            } else {
              core.setFailed('Could not find parent issue reference');
            }

      - name: Create plan sub-issue
        id: create-sub-issue
        uses: actions/github-script@v7
        with:
          script: |
            const { data: subIssue } = await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `[Plan] ${context.payload.pull_request.title.replace('[Spec] ', '')}`,
              body: `## 実装計画フェーズ\n\n親Issue: #${{ steps.find-parent.outputs.parent_issue }}\n\n### 自動実行内容\n- [ ] /plan 実行\n- [ ] /task 実行\n- [ ] /analyze 実行\n- [ ] PR作成\n\n### ステータス\n🔄 処理中...`,
              labels: ['phase:plan', 'status:in-progress']
            });

            core.setOutput('sub_issue_number', subIssue.number);
            return subIssue.number;

      - name: Create plan branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b ${{ steps.extract.outputs.plan_branch }}
          git push -u origin ${{ steps.extract.outputs.plan_branch }}

      - name: Run spec-kit plan, task, analyze
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          cat << 'EOF' > /tmp/plan-prompt.txt
          この機能の実装計画を作成してください。

          ## 指示
          1. /plan を実行して実装計画を作成してください
          2. /task を実行してタスクリストを生成してください
          3. /analyze を実行して整合性を確認してください
          4. 生成されたファイルをコミットしてください
          EOF

          claude --print -p "$(cat /tmp/plan-prompt.txt)" || echo "Claude execution completed"

      - name: Commit plan files
        run: |
          git add -A
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "docs(plan): 実装計画とタスクリストを作成

          Refs #${{ steps.find-parent.outputs.parent_issue }}

          Co-Authored-By: Claude <noreply@anthropic.com>"
            git push
          fi

      - name: Create Pull Request
        uses: actions/github-script@v7
        with:
          script: |
            const { data: pr } = await github.rest.pulls.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `[Plan] ${context.payload.pull_request.title.replace('[Spec] ', '')}`,
              head: '${{ steps.extract.outputs.plan_branch }}',
              base: 'main',
              body: `## 実装計画 PR\n\n### 関連Issue\n- 親Issue: #${{ steps.find-parent.outputs.parent_issue }}\n- 計画Issue: #${{ steps.create-sub-issue.outputs.sub_issue_number }}\n\n### 変更内容\n- 実装計画書の作成\n- タスクリストの生成\n- 整合性分析\n\n### 次のステップ\nこのPRがマージされると、自動的に実装フェーズが開始されます。\n\n---\n🤖 Generated by spec-kit automation`
            });

            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: ${{ steps.create-sub-issue.outputs.sub_issue_number }},
              body: `## 実装計画フェーズ\n\n親Issue: #${{ steps.find-parent.outputs.parent_issue }}\nPR: #${pr.number}\n\n### 自動実行内容\n- [x] /plan 実行\n- [x] /task 実行\n- [x] /analyze 実行\n- [x] PR作成\n\n### ステータス\n✅ PR作成完了 - レビュー待ち`
            });

      - name: Update parent issue
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: ${{ steps.find-parent.outputs.parent_issue }},
              labels: ['status:plan-review']
            });
